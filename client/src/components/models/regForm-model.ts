export class RegFormModel {
  constructor(
    public readonly controlId: string,
    public readonly formLabel: string,
    public readonly formType: string,
    public readonly formPlaceholder: string
  ) {}
}
